# dependencies
node_modules/

# wrangler / cloudflare
.wrangler/
.dev.vars
.dev.vars.*

# editors + os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# logs
npm-debug.log*
yarn-error.log*
*.log

# test + build output
coverage/
dist/

# Screenshots from the browser test scripts. These land wherever the script's
# working directory happens to be, which has twice been the repo root, so the
# rule covers PNGs generally and the few real assets are listed back in.
*.png
!public/art/**/*.png
